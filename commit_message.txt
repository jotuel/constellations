🧪 Add Unit Tests for Error View Component

🎯 **What:** The testing gap for the error view UI component (`view_error` in `src/view/error.rs`) is now addressed. Tests were added to ensure the view renders properly without panics under different states.

📊 **Coverage:** Smoke tests check `view_error` for edge cases like rendering `&str` directly, owned `String`s, empty strings (`""`), and exceedingly long strings.

✨ **Result:** Test coverage improved for the core Error View component, guaranteeing resilience of the element builder pipeline on unexpected string bounds.
